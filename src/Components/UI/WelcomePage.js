import React from 'react';


const WelcomePage = () => {

    return <div>
        <h1>Build / Deployment POC</h1>
        <p>The GitHub Repo housing this app is using Netlify to deploy when the main branch is updated.</p>
    </div>
}

export default WelcomePage;